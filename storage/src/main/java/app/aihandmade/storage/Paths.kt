package app.aihandmade.storage

/**
 * Directory and file names used by the storage helpers.
 */
object Paths {
    const val PROJECTS = "projects"
    const val RUNS = "runs"
    const val EVENTS = "events.jsonl"
    const val MANIFEST = "manifest.json"
    const val ARTIFACTS = "artifacts"
    const val TMP = "tmp"
}
